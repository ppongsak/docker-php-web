version: '2'
services:
  php-web:
    container_name: php-web
    image: pongsak/centos-phpfpm-nginx:latest
    ports:
      - 3000:80
    restart: always
    volumes:
      - "../php-web:/var/www/html"
    working_dir: /var/www/html
